#!/usr/bin/env bash
#set -euo pipefail

# Build Docker image and convert it to an Apptainer/Singularity container.
#REPO_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
#cd "$REPO_ROOT"

#docker build -t dino_wm:latest .
#sudo apptainer build dino_wm.sif docker-daemon://dino_wm:latest
